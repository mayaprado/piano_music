<%= form_for @piece do |form| %>
  Name: <%= form.text_field :name %>
  <br />
  URL: <%= form.text_field :url %>
  <br />
  <%= form.select(:composer_id, Composer.all.collect {|u| [u.name, u.id]}, :prompt => 'Select Composer') %>
  <%= form.submit %>
<% end %>